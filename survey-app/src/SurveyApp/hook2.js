// const getInputValue = (target, isCheckBox) => {
//   const isTargetUndefined = isUndefined(target);
//   return isCheckBox
//     ? isTargetUndefined || isUndefined(target.value)
//       ? target
//       : target.checked
//     : isTargetUndefined || isUndefined(target.value)
//     ? target
//     : target.value;
// };

// var newValue;
//     switch (value) {
//       case "multipleSelection":
//         newValue = $("multipleSelection").find("option:checked");
//         break;
//       case "checkboxChoice":
//         newValue = $("checkboxChoice").find("input:checked");
//         break;
//       default:
//         newValue = event.target.value;
//     }
